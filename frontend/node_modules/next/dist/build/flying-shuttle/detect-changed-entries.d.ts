export interface DetectedEntriesResult {
    app: string[];
    pages: string[];
}
export declare function hasShuttle(shuttleDir: string): Promise<boolean>;
export declare function detectChangedEntries({ appPaths, pagesPaths, pageExtensions, distDir, shuttleDir, }: {
    appPaths?: string[];
    pagesPaths?: string[];
    pageExtensions: string[];
    distDir: string;
    shuttleDir: string;
}): Promise<{
    changed: DetectedEntriesResult;
    unchanged: DetectedEntriesResult;
}>;
